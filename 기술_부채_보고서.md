# 토익 900 단어장 — 기술 부채 보고서

작성일: 2026-07-29 (3차 갱신: 9개 항목 전체 조치 완료)
대상: `js/`, `css/`, `data/`, `sw.js`, `manifest.json`, `index.html`, 문서 파일, 로컬 디스크 사용량 검토

---

## 요약

1차 점검에서 발견한 9개 항목 모두 조치를 완료했습니다. 구조적으로 위험도가 높아 처음엔 보류했던 2개(`app.js` 파일 분리, 자동 테스트 도입)도 진행했고, 대리 수행이 불가능했던 Firestore 규칙 적용도 사용자가 직접 콘솔에서 게시를 완료했습니다.

| # | 항목 | 위험도 | 상태 |
|---|------|--------|------|
| 1 | Firestore 보안 규칙이 6자리 코드 추측만으로 타인 데이터 열람/변조 허용 | 중간 | ✅ 완료 — 90일 미접속 코드 자동 만료 규칙 콘솔에 게시됨 |
| 2 | `app.js` 1,314줄 단일 파일에 렌더링·상태·동기화·TTS 로직 전부 혼재 | 중간 | ✅ 완료 — 책임별 8개 모듈로 분리, 브라우저 회귀 테스트 통과 |
| 3 | 캐시 무효화가 두 곳(HTML `?v=`, SW `CACHE_VERSION`)에서 수동·독립 관리됨 | 중간 | ✅ 완료 |
| 4 | 자동 테스트, 린트, 타입 체크, CI 전무 | 중간 | ✅ 완료 — Node.js 설치 후 16개 테스트 작성·통과 |
| 5 | 8,000단어(2.6MB) JSON을 매 실행마다 무조건 재다운로드 | 낮음 | ✅ 완료 |
| 6 | 가져오기(JSON import) 시 필드 존재만 검사, 타입 미검증 | 낮음 | ✅ 완료 |
| 7 | Firebase SDK를 런타임에 CDN에서 동적 import, 버전 고정 지점이 문자열 하나뿐 | 낮음 | 📝 기록만 (현재 규모에선 시급하지 않음) |
| 8 | 문서(SYNC_SETUP.md)에 이미 이동된 옛 폴더 경로가 남아 있음 | 낮음 | ✅ 완료 |
| 9 | 로컬 `.appdata` 폴더가 4.3GB로 비대화 (크롬 온디바이스 AI 모델 캐시) | 낮음 | ✅ 완료 (4.3GB → 240MB) |

---

## 1. Firestore 보안 규칙이 코드 추측만으로 타인 데이터 접근을 허용

익명 인증은 누구나 즉시 발급받을 수 있어, 유일한 방어선은 6자리 코드(약 12억 조합, [js/sync.js:100](js/sync.js:100))의 추측 난이도뿐이며 만료 로직이 없습니다.

- **조치**: `SYNC_SETUP.md`에 "90일 미접속 코드 자동 만료" 강화 규칙을 추가했습니다.
- **적용 완료**: Firebase 콘솔 로그인이 필요한 계정 설정이라 사용자가 직접 Firestore 규칙 탭에 붙여넣고 게시했습니다. 이제 90일 넘게 동기화가 없었던 코드는 자동으로 접근이 차단됩니다.

## 2. `app.js` 단일 파일에 모든 책임이 혼재 — 완료

기존 [js/app.js](js/app.js)는 1,314줄에 라우팅·전역 상태·동기화·TTS 로직이 모두 섞여 있었습니다. Node.js를 설치해 최소한의 자동 테스트 안전망을 먼저 확보한 뒤(4번 항목), 책임별로 8개 모듈로 분리했습니다.

- **[js/state.js](js/state.js)**: 단어/SRS/화면 상태 저장소 + 순수 조회 함수 (`allWords`, `wordsOf`, `levelStats`, `buildReviewQueue` 등)
- **[js/sync-bridge.js](js/sync-bridge.js)**: 로컬 상태 ↔ Firebase 동기화 연결 (`collectLocalState`, `mergeRemoteState`, `initSync`, `syncPush`)
- **[js/tts.js](js/tts.js)**: 음성 합성 및 듣기 모드 전체
- **[js/views.js](js/views.js)**: 홈/레벨/파트/단어 상세/통계·설정 화면 렌더링
- **[js/study.js](js/study.js)**: 플래시카드 학습 세션(뒤집기/평가/재등장 큐잉)
- **[js/utils.js](js/utils.js) / [js/dom.js](js/dom.js) / [js/ui.js](js/ui.js)**: 순수 유틸, DOM 루트 참조, 토스트/확인 모달
- **[js/app.js](js/app.js)**: 데이터 로드·전역 에러 복구·모듈 배선만 담당하는 35줄짜리 진입점으로 축소

화면 간 상호 호출(예: 학습 종료 시 홈으로, 듣기 종료 시 홈으로) 때문에 `views.js` ↔ `study.js`, `views.js` ↔ `tts.js`, `views.js` ↔ `sync-bridge.js` 사이에 순환 import가 존재하지만, 모든 호출이 이벤트 핸들러 안에서만 일어나 모듈 최상위 실행 시점에는 문제가 없음을 확인했습니다.

- **검증**: 로컬 정적 서버로 앱을 직접 실행해 홈 → 레벨(단일/다중 카테고리) → 파트 → 학습(뒤집기·평가·재등장) → 단어 상세 모달(다음/이전) → 듣기 모드(재생/종료) → 통계·설정(가져오기 성공/실패·초기화) 전체 화면을 클릭해 확인했고, 분리 과정에서 실제로 예문 단어별 뜻 주석(gloss ruby)이 빠지는 회귀와 동기화 실시간 갱신 중복 구현 회귀 2건을 발견해 바로잡았습니다. 의도적으로 예외를 발생시켜 홈 화면 복구도 재확인했습니다.

## 3. 캐시 무효화 불일치 — 완료

서비스 워커 프리캐시 목록과 실제 요청 URL이 서로 다른 캐시 키였던 문제로, 1차 조치에서 `CACHE_VERSION`을 `v3`로 올려 해결했습니다. 이번에 `app.js`가 8개 모듈로 나뉘면서 각 신규 파일에도 `?v=1` 버전 쿼리를 부여하고 [sw.js](sw.js)의 `CORE_ASSETS`와 [index.html](index.html)의 스크립트 태그를 함께 갱신했으며, `CACHE_VERSION`을 `v4`로 올려 이전 캐시를 폐기하도록 했습니다.

## 4. 자동 테스트·린트·CI 전무 — 완료

1차 점검 당시 이 컴퓨터에 Node.js가 없어 보류했던 항목입니다. `winget`으로 Node.js LTS를 설치한 뒤:

- [package.json](package.json)과 [test/srs.test.js](test/srs.test.js), [test/storage.test.js](test/storage.test.js)를 추가해, Node 내장 테스트 러너(`node:test`, 별도 패키지 설치 불필요)로 SRS 알고리즘(박스 승급/강등, 마스터 판정, 반복 횟수)과 저장소 로직(스트릭 계산, 커스텀 단어 중복 제거)에 대한 16개 테스트를 작성했습니다.
- `npm test` (`node --test test/srs.test.js test/storage.test.js`)로 실행해 16개 전부 통과를 확인했습니다.
- 린트/타입체크/CI(GitHub Actions 등)는 이번에 추가하지 않았습니다. 빌드 도구가 없는 순수 정적 사이트 규모에서는 테스트만으로 핵심 로직 회귀는 충분히 잡아낼 수 있다고 판단했고, 필요해지면 이후에 논의하면 됩니다.

## 5. 8,000단어 전체 무조건 재다운로드 — 완료

`init()`에서 `fetch(..., { cache: "no-store" })`로 매번 2.6MB를 강제로 재다운로드하던 것을 제거했습니다. 데이터가 바뀌는 시점(배포)에는 3번 항목과 동일하게 `CACHE_VERSION`을 올리므로 자동으로 최신 데이터를 받습니다.

## 6. 가져오기(JSON import) 타입 미검증 — 완료

[js/views.js](js/views.js)의 가져오기 핸들러에 타입 검증(문자열/배열/숫자), 유효 level 값 검증, id 중복 검증을 추가했습니다. 또한 전역 `error`/`unhandledrejection` 핸들러를([js/app.js](js/app.js)) 추가해 예상치 못한 예외가 나도 흰 화면 대신 토스트 메시지 + 홈 화면 복귀로 처리되도록 했습니다. 브라우저에서 잘못된 level 타입을 넣어 정확한 오류 메시지가 뜨는 것과, 정상 데이터가 실제로 병합되는 것을 모두 재확인했습니다.

## 7. Firebase SDK 런타임 CDN 로드 (기록만)

`SDK_VERSION` 문자열 하나가 3개 모듈의 로드 URL을 결정하며 잠금 파일이나 SRI가 없습니다. 1인 개인 프로젝트 규모에서는 시급하지 않아 조치하지 않았고, 향후 SDK 버전을 올릴 때 이 파일 한 곳만 보면 된다는 점만 기록해 둡니다.

## 8. 문서의 옛 폴더 경로 — 완료

`SYNC_SETUP.md`의 배포 안내가 이동 전 옛 경로(`Desktop\영어단어장`)를 가리키던 것을 `Desktop\제작 앱\영어단어장`로 수정했습니다.

## 9. 로컬 `.appdata` 폴더 비대화 — 완료

[실행.vbs](실행.vbs)의 전용 크롬 프로필 폴더(`.appdata`)에 앱과 무관한 크롬 온디바이스 AI 모델 캐시(4.0GB)가 쌓여 있던 것을 사용자가 직접 삭제해 전체 용량이 4.3GB → 240MB로 줄었습니다. `.gitignore`에 이미 등록되어 있어 저장소와는 무관했습니다.

---

## 남은 항목

없음. 1차 점검에서 발견한 9개 항목 모두 조치를 완료했습니다.
